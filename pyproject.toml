[project]
name = "training-saa"
version = "0.1.0"
description = "AWS SAA-C03 Exam Q&A Streamlit App"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
